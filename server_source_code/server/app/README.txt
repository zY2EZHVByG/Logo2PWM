./static/:
some static files

./templates:
html files and other supporting files.

__init__.py:
Falsk default

utils.py:
Falsk default

views.py:
My code is here. It need package mechanize, flask, and werkzeug
The web app allow user to upload a logo image, 
stores it in ./templates/files/,
Using the matlab code convert the image to PWM (with enologo format, csv format and PSSM in MEME suite motif file format), 
then, submit the matrix to http://www.benoslab.pitt.edu/cgi-bin/enologos/enologos.cgi, and obtain the newly generated sequence logo image.





